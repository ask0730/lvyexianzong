1.利用pinia存储用户登录信息和token
流程：
1.createPinia（）创建Pinia实例store
2.store.use(piniaPluginPersistedstate)添加持久化插件持久化保存状态到localStorage
3.定义setupStore函数，app.use(store)注册Pinia实例
4. useToolStore = defineStore('tool', {})定义一个名为'tool'的store，管理全局工具状态
5.state: () => {},定义store初始状态，是否获取路由、侧边栏是否折叠、用户信息
6.改变store状态actions，isGetterRouter，isCollapsed，userInfo
7.persist持久化key,localStorage,paths: ['isCollapsed', 'userInfo'],

pinia:状态管理工具

pinia生命周期
state
getters
actions
https://fe.ecool.fun/articles/technology/282

pinia和vuex：
pinia：架构分散式，vue3,轻
vuex:中央存储管理应用
