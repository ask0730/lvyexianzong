# 防密码爆破功能说明

## 功能概述

本系统实现了完整的防密码爆破功能，包括前端和后端的双重保护机制，有效防止暴力破解攻击。

## 功能特性

### 前端防爆破功能

1. **登录失败次数限制**
   - 最多允许5次登录失败
   - 超过限制后账户自动锁定5分钟

2. **验证码保护**
   - 3次失败后自动显示验证码
   - 支持点击刷新验证码
   - 验证码区分大小写

3. **账户锁定机制**
   - 实时显示锁定剩余时间
   - 锁定期间禁用登录按钮
   - 状态持久化到localStorage

4. **用户体验优化**
   - 实时倒计时显示
   - 友好的错误提示
   - 响应式设计适配移动端

### 后端防爆破功能

1. **IP级别限制**
   - 单个IP最多10次失败尝试
   - IP锁定时间30分钟
   - 防止分布式攻击

2. **用户级别限制**
   - 单个用户最多5次失败尝试
   - 用户锁定时间30分钟
   - 精确的用户识别

3. **登录尝试记录**
   - 记录所有登录尝试（成功/失败）
   - 包含IP地址、用户代理等信息
   - 自动清理过期记录

4. **管理功能**
   - 查看登录尝试统计
   - 手动解锁用户/IP
   - 查询特定用户/IP的尝试记录

## 技术实现

### 前端实现

#### 核心文件
- `web-admin/src/views/Login.vue` - 登录页面防爆破逻辑
- `web-admin/src/api/index.ts` - API接口定义
- `web-admin/src/views/admin/LoginAttempts.vue` - 管理页面

#### 关键功能
```javascript
// 防爆破配置
const MAX_LOGIN_ATTEMPTS = 5        // 最大尝试次数
const LOCK_DURATION = 5 * 60 * 1000 // 锁定时间5分钟
const CAPTCHA_THRESHOLD = 3         // 验证码阈值

// 状态管理
const loginAttempts = ref(0)        // 失败次数
const isLocked = ref(false)         // 锁定状态
const showCaptcha = ref(false)      // 验证码显示
```

### 后端实现

#### 核心文件
- `web-server/models/LoginAttemptModel.js` - 数据模型
- `web-server/middleware/BruteForceProtection.js` - 防爆破中间件
- `web-server/controllers/admin/LoginAttemptController.js` - 管理控制器
- `web-server/services/admin/LoginAttemptService.js` - 业务逻辑

#### 数据库设计
```javascript
const loginAttemptSchema = new mongoose.Schema({
  username: String,      // 用户名
  ip: String,           // IP地址
  userAgent: String,    // 用户代理
  attemptTime: Date,    // 尝试时间
  success: Boolean,     // 是否成功
  reason: String        // 失败原因
});
```

## 使用方法

### 1. 启动服务

```bash
# 启动后端服务
cd web-server
npm start

# 启动前端服务
cd web-admin
npm run dev
```

### 2. 测试防爆破功能

```bash
# 运行测试脚本
cd web-server
node test-brute-force.js
```

### 3. 管理登录尝试

1. 登录管理后台
2. 进入"系统管理" -> "登录尝试管理"
3. 查看统计信息、锁定用户、尝试记录等
4. 可以手动解锁用户或IP

## API接口

### 登录相关
- `POST /adminapi/user/login` - 用户登录（已集成防爆破）

### 管理接口
- `GET /adminapi/login-attempts/stats` - 获取统计信息
- `POST /adminapi/login-attempts/recent` - 获取最近记录
- `GET /adminapi/login-attempts/locked-users` - 获取锁定用户
- `GET /adminapi/login-attempts/locked-ips` - 获取锁定IP
- `POST /adminapi/login-attempts/unlock-user` - 解锁用户
- `POST /adminapi/login-attempts/unlock-ip` - 解锁IP
- `POST /adminapi/login-attempts/cleanup` - 清理过期记录

## 配置参数

### 前端配置
```javascript
// web-admin/src/views/Login.vue
const MAX_LOGIN_ATTEMPTS = 5        // 最大尝试次数
const LOCK_DURATION = 5 * 60 * 1000 // 锁定时间（毫秒）
const CAPTCHA_THRESHOLD = 3         // 验证码显示阈值
```

### 后端配置
```javascript
// web-server/middleware/BruteForceProtection.js
const BRUTE_FORCE_CONFIG = {
  USER_MAX_ATTEMPTS: 5,     // 用户最大尝试次数
  USER_LOCK_MINUTES: 30,    // 用户锁定时间（分钟）
  IP_MAX_ATTEMPTS: 10,      // IP最大尝试次数
  IP_LOCK_MINUTES: 30,      // IP锁定时间（分钟）
  CLEANUP_HOURS: 24         // 清理过期记录时间（小时）
};
```

## 安全特性

1. **多层防护**
   - 前端验证码 + 后端IP限制
   - 用户级别 + IP级别双重锁定

2. **智能清理**
   - 自动清理过期记录
   - 定期维护数据库性能

3. **详细记录**
   - 完整的登录尝试日志
   - 便于安全分析和审计

4. **灵活管理**
   - 管理员可手动解锁
   - 实时监控和统计

## 注意事项

1. **数据库要求**
   - 需要MongoDB数据库
   - 确保数据库连接正常

2. **性能考虑**
   - 大量并发登录时可能影响性能
   - 建议定期清理过期记录

3. **安全建议**
   - 定期检查锁定用户列表
   - 监控异常登录模式
   - 及时处理安全事件

## 故障排除

### 常见问题

1. **验证码不显示**
   - 检查浏览器控制台错误
   - 确认Canvas API支持

2. **锁定状态不生效**
   - 检查localStorage是否被清除
   - 确认时间同步正常

3. **后端记录不保存**
   - 检查数据库连接
   - 确认模型定义正确

### 调试方法

1. **前端调试**
   ```javascript
   // 在浏览器控制台查看状态
   console.log(localStorage.getItem('loginAttempts'))
   console.log(localStorage.getItem('lockEndTime'))
   ```

2. **后端调试**
   ```javascript
   // 查看登录尝试记录
   db.loginattempts.find().sort({attemptTime: -1}).limit(10)
   ```

## 更新日志

- v1.0.0 - 初始版本，实现基础防爆破功能
- 支持前端验证码、账户锁定
- 支持后端IP限制、用户锁定
- 支持管理界面和统计功能 